# Backend configuration
HOST=0.0.0.0
PORT=8080

# Demo token for /ingest (optional). If empty, ingest is open.
INGEST_TOKEN=

# Optional: Arduino serial port (Windows example: COM6)
# SERIAL_PORT=COM6
